#!/usr/bin/env bash

#MISE description="Build the {{ project-name | capitalize }} application (Release)"
#MISE sources=["Cargo.toml", "sqlc.yaml", "src/**/*.rs", "assets/**", "queries"]
#MISE outputs=["target/release/{{ project-name }}", "target/frontend/**"]
#MISE depends=["test", "generate:sql"]

echo "🚀 Starting {{ project-name | capitalize }} release build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

cargo build --release
