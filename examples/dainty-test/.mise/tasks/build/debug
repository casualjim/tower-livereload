#!/usr/bin/env bash

#MISE description="Build the {{ project-name | capitalize }} application (Debug)"
#MISE sources=["Cargo.toml", "sqlc.yaml", "src/**/*.rs", "assets/**", "queries"]
#MISE outputs=["target/debug/{{ project-name }}", "target/frontend/**"]
#MISE depends=["generate:sql"]

echo "🚀 Starting {{ project-name | capitalize }} debug build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

cargo build
