#!/usr/bin/env bash

#MISE description="Build the {{ project-name | capitalize }} docker image"
#MISE sources=["dist/**"]
#MISE depends=["test", "build", "build:dist"]

echo "🚀 Starting {{ project-name | capitalize }} release build..."
proj_dir=$(git rev-parse --show-toplevel)
cd "$proj_dir" || exit 1

docker build --pull -t ghcr.io/casualjim/{{ project-name }}:latest -f Dockerfile .
